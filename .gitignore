__pycache__/
node_modules/
# dist/ is committed: the plugin loads it without a build step.
relay-evidence/
test-evidence/
.claude/
# CI checks vm-service out here to build vmctl for the parity test.
.ci/
.release/
